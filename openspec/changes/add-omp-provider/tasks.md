## 1. Helpers and wiring

- [x] 1.1 Extend the neutrality test: `src/core/**` fails on `.omp`, `PI_CONFIG_DIR` or `PI_CODING_AGENT_DIR`; `src/providers/**` fails on importing `node:sqlite`. Verify it passes, and fails when a core file is given a `.omp` string or a provider imports `node:sqlite`.
- [x] 1.2 Add optional `tty(pid)` to `Processes` and implement it locally (`ps -o tty=` on macOS, `/proc/<pid>/stat` → device name on Linux), returning the name below `/dev` or nothing. Verify a test that spawns a child on a pty gets its tty, a piped child gets nothing, a dead pid gets nothing, Claude/Grok/Codex never call it, and a fake clock advancing arms no timer.
- [x] 1.3 Add `src/helpers/sqlite.ts`: a read-only `query(path, sql, params)` over a dynamically imported `node:sqlite`, opening and closing per call, absent when the import fails. Thread `sqlite?` through `InstanceOptions`, `WatchContext`, `ListContext` and `InspectContext`. Verify a test where another process holds a WAL database and commits a row then reads that row, the writer is not blocked, a simulated missing `node:sqlite` leaves `sqlite` undefined without throwing, and no connection stays open after a call.
- [x] 1.4 Add the `heldOpen` option to `watchFile`: also watch the file itself, re-attach when the parent reports the name created or replaced, same debounce and churn handling. Verify the three spec scenarios (WAL commits by a long-lived `sqlite3` child are reported; delete-and-recreate keeps delivering; without the option only the parent is watched) and that the existing `watchFile` tests still pass.
- [x] 1.5 Add `OhMyPi` to `Harness`, and `src/providers/oh-my-pi/` with an `ohMyPi(options)` factory returning a `Provider` (`id` `oh-my-pi`, `harness` `OhMyPi`) with empty `watch`/`list`/`inspect`, exported from its `index.ts`. Verify a type test imports it.

## 2. Paths and registry parsing

- [x] 2.1 `paths.ts`: home from option, then `~/<PI_CONFIG_DIR>`, then `~/.omp`; agent dir from `PI_CODING_AGENT_DIR`; the Linux XDG mapping copied from `utils/src/dirs.ts` at the pinned commit; roots = home plus `profiles/*`; session id and timestamp from `<ISO-ts>_<uuid>.jsonl`; artifact directory from the session path. Verify table tests for each override, a custom `home` ignoring the env, a profile root, a malformed file name, and the XDG case.
- [x] 2.2 `registry.ts`: bounded parsers for a presence file (`pid` positive integer, `projectDir`) and a breadcrumb (cwd line, session path, `fresh`, ignore `cwdstat`), plus the terminal-name check (`ttys*`, `pts-*`, `tty*`; reject `zellij-`/`tmux-`/`cmux-`/`kitty-`/`wezterm-`/`apple-`) and the tty → breadcrumb-name mapping (`/` → `-`). Verify tests for invalid JSON, oversize input, a breadcrumb with and without `fresh`, a two-line breadcrumb, a non-tty name, and `pts/3` → `pts-3`.
- [x] 2.3 `session-file.ts`: bounded head read giving the title slot (line 1) and the `session` header (line 2), with id-equals-filename validation. Verify tests for a real head, an empty slot, a `user`-sourced slot, a header id mismatch, a malformed line 2, and an oversized head.

## 3. Transcript reducer and mapping

- [x] 3.1 `events.ts`: pure reducer over entries producing status, turn facts, title and model per the Status mapping and Turn activity requirements, including `ask` → `waiting`, the `tool_execution_start` marker not double-starting a tool, a turn started by an assistant/`toolResult` record when none is open, and `session_exit` ending an open turn `interrupted`. Verify table tests for every `stopReason`, the ask sequence, retry after `error`, `isError` tool results not ending the turn, `developer`/`fileMention`/`custom_message` leaving status unchanged, and an unknown entry type.
- [x] 3.2 Synthetic first turn: the reducer accepts "prompt submitted" from history for a session with no transcript, and reconciles it when the transcript backlog arrives. Verify the four first-turn scenarios (running from history, completed once not twice, failed from `error`, backlog rows at start ignored).
- [x] 3.3 `journal.ts`: the pure record mapper for `inspect` (`user`, `assistant` with `provider/model`, `tool`, `tool-result`, `error` + `turn-end`, `title`, `subagent`, `subagent-end`), skipping every bookkeeping type named in the spec. Verify tests for `thinking` unmapped, `async-result` not a prompt, `title_change` → `title`, and unknown types unmapped.
- [x] 3.4 Compat test with a checked-in list of entry types and `stopReason` values copied from `session-entries.ts` and `ai/src/types.ts` at the pinned commit (no runtime read of the omp checkout): every type the reducer acts on is in that list. Verify it fails if the reducer is given a rule for a type not in the list.

## 4. Watch, bind, and lifecycle

- [x] 4.1 `provider.ts` `watch`, per root: `watchDir` on `agent/terminal-sessions`, on `run/daemons`, and on each `<hash>/clients` as it appears; bind from either side when a live presence pid's `tty` names a valid, fresh-enough breadcrumb; create vs open from `fresh`/file existence; `watchProcess` once per pid; close on presence delete or exit, whichever first. Verify provider tests with fake `processes` (including `tty`) and a temp home for: launch with a `fresh` breadcrumb → create with pid, cwd and id before any transcript; already running at start → open in catch-up; presence serviced before breadcrumb and the reverse; old breadcrumb with no process; breadcrumb older than its process; two pids on one tty; headless presence with no breadcrumb emits nothing; corrupt presence/breadcrumb; header id mismatch; no `tty` implementation → no live rows and history still listed; kill -9 (exit event and `reconcile` paths) with stale files binding nothing after; `agent.db`, `models.db`, `logs/` and `.lock` files never read (spy); and a fake clock advancing with no close.
- [x] 4.2 Session switch and relocation: a breadcrumb rewrite naming another session id → close then create/open with the same pid and no carried state; the same id at a new path → swap the tail, silent replay, `session:update` on cwd change. Verify tests for `/new`, `/resume` to an existing session, and a move that re-opens no finished subagent.
- [x] 4.3 Per-session directory watch: transcript appearing, rename-replace (close tail, re-open, silent replay), artifact directory appearing; events filtered by the two names. Verify a replace followed by appends handles each appended record once with no `session:close`, and a sibling session's writes cause no read of the bound transcript (spy).
- [x] 4.4 Profiles: watch `<home>/profiles`, run the watch set per root, merge by session id. Verify a session under `profiles/work`, a profile created after start, and the same id under two roots reported once.
- [x] 4.5 Ordering: breadcrumb and presence events serviced one at a time per key; stop or close during bind leaves no watches. Verify the two spec scenarios (process exits while the backlog is read; stopped while binding).

## 5. Activity, first turn, titles, subagents

- [x] 5.1 `activity.ts`: feed the reducer from the transcript tail; silent replay at bind; a turn left open before the bound process started ends `interrupted` at process start; an open turn ends `interrupted` on close. Verify a 40-turn replay emits one `session:activity`, the killed-process scenario, and a failed turn keeping `activity.error` until the next turn.
- [x] 5.2 `history.ts`: when `ctx.sqlite` exists, read `max(id)` once at start, hold a `heldOpen` `watchFile` on `agent/history.db-wal`, and on each change query `id, created_at, cwd, session_id, prompt FROM history WHERE id > ?`; rows for bound sessions with no transcript feed 3.2; a cursor that went backwards (GC rebuild) resets to `max(id)` without replaying. Verify with a real SQLite fixture written by a helper process: first prompt → `running` + prompt title before a transcript exists; rows for unbound or materialised sessions ignored; missing reader → no error and `idle`; a missing column → one `error` event and files still observed; `agent.db` never opened.
- [x] 5.3 Titles and model: `title_change` (`user` vs `harness`), prompt title from transcript or history, model from `model_change` / latest assistant `provider/model` at bind and on change; the slot is not re-read live. Verify a generated title replacing the prompt title, a user title winning, and a mid-session model switch firing `session:update`.
- [x] 5.4 `subagents.ts`: watch the artifact directory, start a subagent per `*.jsonl` (ignore `*.md`, `*.log`, `*.json`), fields from the child header and `session_init`, `parentId` from `parentSession`, `background` flipping when the root turn ends first, completion by first match (`yield` success / `stop` with no open tool → completed, `error` → failed, `aborted` or `session_exit` while open → cancelled), one end per child, no restart of an ended child, ended children at bind reported by `subagents()` without `subagent:start`. Verify every Subagent lifecycle scenario, including yield-then-exit yielding one end, nested `parentId`, catch-up of a running child, and a subagent transcript not affecting root activity.

## 6. History and inspect

- [x] 6.1 `list.ts`: walk `<agent>/sessions/*/*.jsonl` in every root (never artifact subdirectories), `since` short-circuit on mtime, bounded head for id/cwd/startedAt/slot title, first user prompt only when the slot is empty, bounded tail for model, `kind` absent. Verify subagent exclusion, slot title without parsing the body (spy on bytes read), cwd from the header not the directory name, a malformed head skipped, and the same id in two roots listed once.
- [x] 6.2 `inspect`: `transcript()` / `events()` replay and tail mapped records for sessions and subagents, honouring `AbortSignal`, surviving a rename-replace while following. Verify appending a user record yields a new `user` event, `break`/`close()` releases the watch, and a closed session's subagents all have final statuses.

## 7. Conformance and fixtures

- [x] 7.1 Write `src/testing/omp-driver.ts` implementing `FixtureDriver` against omp's files: presence file + breadcrumb + a fake `tty` map for the fake `Processes`, transcripts with the 256-byte slot and header, `stopReason`-based phases, `tool_execution_start` markers, child transcripts with `session_init` and `yield` results, `switchConversation` as a breadcrumb rewrite, `relocateJournal` as a same-id breadcrumb rewrite, `remove` as presence delete. It writes only what real omp writes. Export it from `src/testing/index.ts`. Verify it compiles.
- [x] 7.2 Run `defineConformanceTests` against `ohMyPi({ home })` with that driver. Verify every conformance case passes.
- [x] 7.3 Capture scrubbed real omp fixtures under `test/fixtures/oh-my-pi/`: a multi-turn transcript with tools, the three-subagent run (parent plus children), a nested-subagent run (recording where nested transcripts land and updating design.md's open question), a provider-error turn, an aborted turn, an `ask` wait, a presence file, a `fresh` and a materialised breadcrumb, and a `history` table dump. Verify grep finds no home path, original prompt text, or credential material.
- [x] 7.4 Add expected-output snapshots for mapping, activity, subagents and listing of those fixtures, plus a compat test pinning the header, presence and breadcrumb fields we read. Verify the tests pass.

## 8. Packaging, docs, live tests

- [x] 8.1 Add `ohMyPi` to `builtInProviders` and re-export it from `src/index.ts`. Update the pack test, `test/types/readme.ts` and `test/types/usage.ts`. Verify an `npm pack` install imports every entry point and `--json` is `[]` when all four homes point at empty temps.
- [x] 8.2 README: a compatibility-matrix row (Status 🟢 with a footnote that a new session's first turn needs Node ≥ 22.13; Waiting for 🟠, `ask` only; Print mode 🟠, history only; the rest 🟢) and an oh-my-pi section parallel to the others (home and profiles, registry join by tty, status from `stopReason`, first turn from `history.db`, files never opened). Keywords include `omp` and `oh-my-pi`. Verify README snippets still typecheck.
- [x] 8.3 E2e helpers: run omp on a pty under a scratch `$HOME` whose config puts every model role on OpenRouter Haiku (`OPENROUTER_API_KEY`, model env-overridable via `AYA_E2E_OMP_MODEL`), and install omp in the CI e2e job. Verify helpers compile and live tests skip without `AYA_LIVE=1`.
- [x] 8.4 Opt-in live smoke (`AYA_LIVE=1`): launch omp on a pty, wait for `session:create`, send a prompt, wait for `running` before the transcript exists (Node ≥ 22.13), then `idle`, quit, wait for `session:close`, and find it in `sessions({ since: startOfToday })`. Verify it passes locally and is skipped by default.
- [x] 8.5 Opt-in live subagents: a prompt that spawns three task subagents; wait for three `subagent:start` and three `subagent:end` `completed`. Verify it passes locally and is skipped by default.
- [x] 8.6 `npm test`, `npm run lint` and `npm run test:coverage` pass. Verify no `setInterval` was added outside `helpers/coalesce.ts`, and `openspec validate add-omp-provider --strict` passes.

## 9. Review follow-ups

- [x] 9.1 History cursor also tracks `created_at`, so a prompt typed before (omp upserts on the prompt text, keeping the row id) still starts a new session's first turn. Verify with omp's own upsert statement.
- [x] 9.2 `PI_CODING_AGENT_DIR` turns XDG redirects off, as omp does; `omp -p` on a terminal is documented as live `interactive`.
- [x] 9.3 Pull the history cursor (`history.ts`), seeded replay (`activity.ts`) and subagents-from-disk (`subagents.ts`) out of `provider.ts`.
- [x] 9.4 `subagents()` from history: `background` from the parents' `task` results, and a child with no end of its own takes its parent's report.
- [x] 9.5 Custom session files: relative breadcrumb paths resolved against the cwd, custom names identified by their header, listed from `custom-session-files/`, no artifact directory for a non-`.jsonl` name.
- [x] 9.6 Profiles found under XDG too, and a deleted profile closes its sessions.
- [x] 9.7 Live e2e on OpenRouter Haiku in an isolated home, run by CI with omp installed through Bun.

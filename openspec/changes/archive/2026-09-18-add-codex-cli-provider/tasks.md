## 1. Wiring, neutrality, and holders

- [x] 1.1 Extend the neutrality test so `src/core/**` also fails on `.codex` or `CODEX_HOME`. Verify it still passes, and fails when a core file is given a `.codex` string.
- [x] 1.2 Add optional `holders(path)` and `heldUnder(directory)` to `Processes`. Implement them on the local helper (`lsof` on macOS, `/proc/*/fd` on Linux). Verify a test that opens a temp file in a child process returns that pid once, a missing implementation is not called by Claude/Grok, and a fake clock advancing arms no timer.
- [x] 1.3 Add `src/providers/codex-cli/` with a `codexCli(options)` factory returning a `Provider` (`id` `codex-cli`, `harness` `Codex`) that implements empty `watch`/`list`/`inspect`. Export it from `src/providers/codex-cli/index.ts`. Verify a type test imports it.

## 2. Paths and session meta

- [x] 2.1 `paths.ts`: home from option, then `CODEX_HOME`, then `~/.codex`; parse `rollout-<ts>-<thread-id>.jsonl` and the revert form `rollout-<ts>-<thread-id>_<rollout-id>.jsonl` (and `.jsonl.zst`). Verify table tests for the env override, a custom `home`, a normal filename UUID, a revert suffix using the thread id not the rollout id, and stripping `.zst`.
- [x] 2.2 `session-meta.ts`: bounded first-line parse, UUID `id`, root vs subagent (`parent_thread_id` / `thread_source` / `source.subagent`), `cwd`, `source` → kind, `timestamp` vs process start minus 5s with unknown-start fallback and no upper bound. Verify tests for recycled pid, 60 s after start accept, 4 s before start accept, 6 s before start reject, malformed JSON, oversized first line, a subagent meta not treated as a root, and a user fork (`forked_from_id` only) treated as a root.

## 3. Status, events, and journal mapping

- [x] 3.1 `status.ts`: map `task_started`/`turn_started` → `running`; `task_complete`/`turn_complete`/`turn_aborted` or no open turn → `idle`; every other type (including `item_completed`, `token_count`, `thread_settings_applied`, and any transient type that should never appear) leaves status unchanged; never `waiting`. Verify table tests including a settings-only log staying idle, and an open tool call with no output staying `running` with no `waitingFor`.
- [x] 3.2 `events.ts`: pure reducer over the rollout producing status and turn facts: turn start/end from lifecycle `event_msg`, tools from `response_item` calls and their `*_output` by `call_id`, failure from `task_complete.error`. `item_completed` never starts or finishes a tool. Verify tests for `task_complete.error` → failed with message, `turn_aborted` → interrupted, tool id from `call_id` with name fallback, and one `exec` call plus two `CommandExecution` items reporting one start and one finish.
- [x] 3.3 `journal.ts`: bounded rollout lookup (derived date path, then one-level filename search, ambiguity → none) and the pure `response_item` mapper. Verify tests for two files with the same id, `reasoning`/encrypted/`world_state` unmapped, `function_call`/`custom_tool_call`/`local_shell_call` → `tool`, `compacted` internals skipped, `item_completed` duplicates producing no second event, and a `legacy`-mode rollout (no `item_completed` items) mapping to the same conversation.
- [x] 3.4 Compat test with a checked-in list of persisted `event_msg` types copied from `codex-rs/rollout/src/policy.rs` at the pinned commit (no runtime read of the Codex checkout): every type the reducer acts on is in that list. Verify it fails if the reducer is given a rule for a transient type.

## 4. Watch, bind, and lifecycle

- [x] 4.1 `provider.ts` `watch`: `watchDir` on `sessions/` and each year/month/day dir the scan (and later creates) report; at start, one `heldUnder(sessions/)` to bind currently open roots; later, `holders(path)` on create/change of an unbound `rollout-*.jsonl`. Bound map keyed by thread id. Diff: newly held → bind, no longer held → close, cwd change → update. Conversation switch is drop A + hold B. A close raises no notification, so on every bind or notification for pid P re-probe `holders` for P's other bound rollouts in the same pass. Bind: validate meta, skip subagent roots, `watchProcess` refcounted per pid, create vs open from file existence, tail the rollout. Verify provider tests with fake `processes` (including `heldUnder`/`holders`) and a temp home for two sessions in one cwd, two sessions sharing one pid, auth/sqlite/ipc/chat_processes never read (spy), missing home then file created and held, conversation switch as close then open without carrying state (A closed by the re-probe that binding B triggers, with no notification on A), a quietly dropped rollout staying live until the next notification for that pid or `reconcile` with no timer armed, a resume before start bound by the start probe, a resume after start bound on first append, a subagent rollout held by the same pid not emitted as a root, kill -9 closing every session of that pid (exit event and `reconcile` paths), a stale file later held by a new process, no holders implementation yielding no live rows, and a fake clock advancing with no close.
- [x] 4.2 Relocation: on same-id path change (including `thread/revert` writing `_<rollout-id>`), swap the tail, silent replay; `thread_settings_applied` cwd → `session:update`. Verify a move test and a revert-suffix test emit no `session:close` / no second create, tail the new path, and re-open no finished subagent.
- [x] 4.3 Date-directory events serviced in order: a rewrite or removal during bind cannot overtake it; stop during bind leaves no watches. Verify the three spec scenarios (file removed while binding, process exits while backlog is read, stopped while binding).

## 5. Activity, model, titles, subagents

- [x] 5.1 `activity.ts`: map lifecycle `event_msg` and `response_item` tool calls to turn facts; end an open turn `interrupted` on session close; silent replay at bind; `task_complete.error` sets `lastTurn` failed with `error.message`. Verify a 40-turn replay emits one `session:activity`, bind after `turn_aborted` with an unanswered `function_call` yields no open tool and `lastTurn` `interrupted`, a settings-only rollout does not start a turn, and a failed turn keeps `activity.error` until the next `task_started`.
- [x] 5.2 Model and titles: model from `thread_settings_applied` / `turn_context` at bind then on change; `session_index.jsonl` `thread_name` → `harness`, first real user `response_item` → `prompt` (200 chars). Verify live session with no index title uses the prompt, a later `thread_name` wins, and a mid-session model switch fires `session:update`.
- [x] 5.3 `subagents.ts`: link child rollouts by `session_meta.parent_thread_id` (sufficient alone), with `item_completed` `CollabAgentToolCall` `spawn_agent` `receiver_agents[].thread_id` as an optional earlier hint; no `collab_*` events. type/title/parentId from child meta; `background` flips true when the parent turn ends with the child's open; nothing cancelled on parent idle; completion from child `task_complete` (`error` → failed) / `turn_aborted` (→ cancelled) or parent `wait`/`close_agent` `agents_states`, first wins, one `subagent:end`; an ended child is not restarted by later input; never emit a subagent rollout as a root. Verify every Subagent lifecycle scenario, including both link orders yielding one start, a `legacy`-mode parent with no collab items, wait-plus-child completion yielding one end, catch-up of a running background child, nested parentId, and subagent transcript not affecting root activity.

## 6. History, inspect, headless

- [x] 6.1 `list.ts`: walk `sessions/*/*/*/rollout-*.jsonl` (and `.jsonl.zst` by name only), skip subagent meta, skip `archived_sessions`, one snapshot per thread id (revert suffixes collapse), bounded first-line metadata on plain files, `since` short-circuit on mtime, titles from `session_index.jsonl` then first user prompt. Verify subagent exclusion, index title over prompt, headless kind for `source` `exec`, a zst-only thread listed without being opened, and a large rollout not fully parsed when the index already has a title.
- [x] 6.2 `inspect`: `transcript()` / `events()` replay and tail mapped records for sessions and subagents; honor `AbortSignal`. Verify appending a user record yields a new `user` event, `break`/`close()` releases the watch, and a closed session's subagents all have final statuses.
- [x] 6.3 Headless: `codex exec` is live while held, then listed with `kind` `headless`. Verify a print-mode fixture via `sessions({ since, kind: 'headless' })`.

## 7. Conformance and fixtures

- [x] 7.1 Write `src/testing/codex-driver.ts` implementing `FixtureDriver` against Codex files (date-tree rollouts, `task_started`/`task_complete` phases, `response_item` records, child rollouts, `session_index.jsonl`) plus an in-memory holders map for the fake `Processes`. It writes only record types real Codex persists: `failTurn` appends `task_complete` with `error`, subagent helpers write child rollouts (no `collab_*` events), `switchConversation` and `remove` rely on the create/delete notification to trigger the re-probe. Export it from `src/testing/index.ts`. Verify it compiles.
- [x] 7.2 Run `defineConformanceTests` against `codexCli({ home })` with that driver. Verify every conformance case passes.
- [x] 7.3 Capture scrubbed real Codex CLI fixtures under `test/fixtures/codex-cli/` (root rollout, multi-turn events + response items, `item_completed` items, collab children with nicknames plus the parent's `spawn_agent`/`wait` items, an aborted turn, a `task_complete.error` turn (synthetic and marked so if one cannot be provoked), no approval fixture because approvals are not persisted, `session_index.jsonl` title, `source: exec` snippet). Verify grep finds no home path, original prompt text, or `encrypted_content`.
- [x] 7.4 Add expected-output snapshots for mapping, activity, subagents, and listing of those fixtures, plus a compat test pinning the allowed `session_meta` fields. Verify the tests pass.

## 8. Packaging, docs, live tests

- [x] 8.1 Add `codexCli` to `builtInProviders` and re-export it from `src/index.ts`. Update pack test, `test/types/readme.ts`, and `test/types/usage.ts`. Verify `npm pack` install still imports all entry points and `--json` is `[]` when `CODEX_HOME`, `GROK_HOME`, and `CLAUDE_CONFIG_DIR` point at empty temps.
- [x] 8.2 README: Codex CLI section parallel to Claude Code and Grok Build (home, live from open rollouts, status from turn lifecycle events, files never opened) and its limits: no `waiting` status because Codex does not persist approvals, no session until the first prompt, a resume shown on its first append, a quietly unloaded VS Code thread staying listed until the next event. Keywords include `codex`. Verify README snippets still typecheck.
- [x] 8.3 E2e helpers: isolated `CODEX_HOME` with an OpenRouter `model_providers.openrouter` (`wire_api = "responses"`, since Codex rejects `chat`; `x-ai/grok-4.6`, `OPENROUTER_API_KEY`; top-level keys written before the provider table), `codex exec -s danger-full-access --dangerously-bypass-approvals-and-sandbox`. First verify by hand that one real `codex exec` completes a tool-calling turn through OpenRouter's Responses endpoint with that model; if not, pick a model/endpoint that does and keep it env-overridable. Verify helpers compile and live tests skip without `AYA_LIVE=1`.
- [x] 8.4 Opt-in live smoke (`AYA_LIVE=1`): start `codex exec`, wait for create/open → status → close, and `sessions({ since: startOfToday })`. Verify it passes locally and is skipped by default.
- [x] 8.5 Opt-in live subagents: prompt that spawns three collab/subagents, wait for three `subagent:start` and three `subagent:end`. Verify it passes locally and is skipped by default.
- [x] 8.6 `npm test`, `npm run lint`, and `npm run test:coverage` pass. Verify no `setInterval` was added outside `helpers/coalesce.ts`.
